import 'package:flutter/material.dart';

class AngularCourse extends StatelessWidget {
  const AngularCourse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("No Angular Courses Yet"),
      ),
    );
  }
}