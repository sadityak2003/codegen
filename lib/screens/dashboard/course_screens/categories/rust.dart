import 'package:flutter/material.dart';

class RustCourse extends StatelessWidget {
  const RustCourse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("No Rust Courses Yet"),
      ),
    );
  }
}