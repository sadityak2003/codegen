import 'package:flutter/material.dart';

class ScalaCourse extends StatelessWidget {
  const ScalaCourse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("No Scala Courses Yet"),
      ),
    );
  }
}