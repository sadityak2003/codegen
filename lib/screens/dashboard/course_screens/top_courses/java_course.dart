import 'package:flutter/material.dart';

class JavaCourse extends StatelessWidget {
  const JavaCourse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("No Java Courses Yet"),
      ),
    );
  }
}