import 'package:flutter/material.dart';

class CPPCourse extends StatelessWidget {
  const CPPCourse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("No C++ Courses Yet"),
      ),
    );
  }
}