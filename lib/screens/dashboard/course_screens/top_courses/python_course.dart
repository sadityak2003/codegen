import 'package:flutter/material.dart';

class PythonCourse extends StatelessWidget {
  const PythonCourse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("No Python Courses Yet"),
      ),
    );
  }
}